name: Reusable Release

on:
  workflow_call:
    inputs:
      ffversion:
        required: true
        type: string

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: get latest release version
        id: get_version_release
        uses: pozetroninc/github-action-get-latest-release@master
        with:
          repository: ${{ github.repository }}

      - name: download artifacts
        if: inputs.ffversion != steps.get_version_release.outputs.release
        uses: actions/download-artifact@v8

      - name: create release
        if: inputs.ffversion != steps.get_version_release.outputs.release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ inputs.ffversion }}
          commit: ${{ github.sha }}
          artifactErrorsFailBuild: true
          artifacts: autofetch-*/autofetch-*
          body: "Please refer to [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/${{ inputs.ffversion }}/CHANGELOG.md) for details."

      - name: download source tarballs
        if: inputs.ffversion != steps.get_version_release.outputs.release
        run: |
          for i in 1 2 3 4 5; do curl -L --remote-name-all --output-dir autofetch-source --create-dirs https://github.com/${{ github.repository }}/archive/refs/tags/${{ inputs.ffversion }}.{tar.gz,zip} && break || sleep 5; done
          ls autofetch-*/*

      - name: generate release notes
        if: inputs.ffversion != steps.get_version_release.outputs.release
        run: |
          echo "Please refer to [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/${{ inputs.ffversion }}/CHANGELOG.md) for details." > autofetch-release-notes.md
          echo -e "\n---\n\n<details><summary>SHA256SUMs</summary><br>\n\n\`\`\`" >> autofetch-release-notes.md
          sha256sum autofetch-*/* >> autofetch-release-notes.md
          echo -e "\`\`\`\n</details>" >> autofetch-release-notes.md
          echo -e "\n<details><summary>SHA512SUMs</summary><br>\n\n\`\`\`" >> autofetch-release-notes.md
          sha512sum autofetch-*/* >> autofetch-release-notes.md
          echo -e "\`\`\`\n</details>" >> autofetch-release-notes.md

      - name: update release body
        if: inputs.ffversion != steps.get_version_release.outputs.release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ inputs.ffversion }}
          commit: ${{ github.sha }}
          bodyFile: autofetch-release-notes.md
          allowUpdates: true
