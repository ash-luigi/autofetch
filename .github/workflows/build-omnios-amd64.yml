name: Reusable OmniOS amd64

on:
  workflow_call:

env:
  CMAKE_BUILD_TYPE: ${{ vars.CMAKE_BUILD_TYPE || 'RelWithDebInfo' }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v6

      - name: run VM
        uses: vmactions/omnios-vm@v1
        with:
          usesh: true
          envs: 'CMAKE_BUILD_TYPE'
          prepare: |
            uname -a
            pkg update --accept || true
            pkg install gcc14 cmake git pkg-config glib2 dbus sqlite-3 imagemagick ninja

          run: |
            cmake -DSET_TWEAK=Off -DBUILD_TESTS=On -GNinja .
            cmake --build . --verbose -j4
            ./autofetch --list-features
            time ./autofetch -c presets/ci.jsonc --stat false
            time ./autofetch -c presets/ci.jsonc --format json
            time ./flashfetch
            ldd autofetch
            ctest --output-on-failure
            echo 'set(CPACK_PACKAGE_FILE_NAME "autofetch-omnios-amd64")' >> CPackConfig.cmake
            cpack -V

      - name: upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: autofetch-omnios-amd64
          path: ./autofetch-*.*
